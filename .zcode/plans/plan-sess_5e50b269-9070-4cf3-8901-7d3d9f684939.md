Executing the plan at `docs/superpowers/plans/2026-07-09-claim-grounded-retag-readiness.md` — Claim-Grounded Retag Readiness.

**Goal:** Make auto-tagged critique output trustworthy before bulk retagging by grounding generated claims in visible observations, DOM facts, or explicit extraction evidence.

**7 implementation tasks plus post-review hardening, TDD-first, small commits:**

1. **Schema + shared a11y helpers** (`src/schema.ts`) — `AccessibilityRisk` union (legacy strings + structured objects with required `evidence`), `accessibilityRiskTextFields()`, `formatAccessibilityRisk()`, update `findDraftMarkers`/`entryTextFields`.

2. **Evidence-gated a11y sanitizer** (`src/tagger.ts`) — rewrite `sanitizeAccessibilityRisks` with quality gates: drop short evidence, generic component names, palette-only evidence, icon-only risks when evidence shows labels, color-only risks without concrete UI evidence; cap non-DOM risks at 2; downgrade model-emitted `dom-grounded` to `inferred`.

3. **Claim-grounded critique prompt + palette stripping** (`src/tagger.ts`) — `critiqueSafeExtraction()` strips `dominantColors`/`accentColor`/`colorRoles` before `buildCritiquePrompt`; broaden grounding rules to all prose fields (critique, steals, anti-patterns, voice, business rationale).

4. **Structured risk rendering** (`src/server.ts`, `src/embeddings.ts`, `ui/app.js`, `ui/classic-app.js`) — object-aware display with evidence as a muted second line.

5. **`calculator` pattern type** (`src/schema.ts`, `src/tagger.ts`) — new enum value + calibration rule + migration/test fixture updates.

6. **Id-only rename endpoint + UI control** (`src/scripts/ui-server.ts`, `ui/app.js`) — `POST /api/entries/:id/rename`, no image rename.

7. **Retag readiness smoke gates** — build/test/validate-corpus, targeted Wise re-tag (confirm both fake risks gone + calculator pattern), 10-entry mixed smoke, rebuild index, then go/no-go on bulk retag.

**Post-review hardening:** persist `patternDiscovery` on corpus entries instead of transient `_raw`, keep structured accessibility risks intact in the classic editor, reject punctuation-only rename ids instead of normalizing to `sample`, and make extraction-only `_raw.extractionProvider` honor explicit overrides.

**Commits:** small focused commits as listed in the plan's commit section.

**Key constraint:** No full corpus retag until smoke gates pass.
