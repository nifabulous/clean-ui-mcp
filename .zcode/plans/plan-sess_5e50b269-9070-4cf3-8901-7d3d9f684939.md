Implementing the user's approved plan "Platform-Scoped Mobile Taxonomy Fix" — adding bottom-nav + action-list to the Component enum, platform-aware normalization of components/layout, injecting detected platform into the extraction + critique prompts, and extending deferred critique. Tests for all platform permutations.