# Rung 1 only. Deliberately excludes the model stack so the classical baseline
# can run with no torch download — see requirements-rung2.txt.
# 1.26.4, NOT 2.1.3: ultralytics (rungs 3b/3c) requires numpy<2.0.0 on darwin,
# so installing it downgrades numpy. Pinning 2.1.3 here would have described an
# environment no rung-3 install can reach. Verified 2026-08-08: rung 1 re-run
# under 1.26.4 reproduces its committed verdict exactly (23.9% global; per-field
# 0.167/0.200/0.250/0.333; failing checks 3/32/0/19).
numpy==1.26.4
pillow==11.0.0
opencv-python-headless==4.10.0.84
pytest==8.3.4
